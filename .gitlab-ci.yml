services:
  - docker:dind

variables:
  DOCKER_HOST: tcp://docker:2375
  DOCKER_DRIVER: overlay2

build:
  image: maven:3.6.1-jdk-11
  stage: build
  script:
  - mvn clean install
  artifacts:
    paths:
      - target/*.jar